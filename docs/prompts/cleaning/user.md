Input: JSON representing a parsed page. Output: JSON with `document_id`, `page_number`, and `segments`.
Each segment must include `segment_id`, `text`, `needs_review`, and `rationale` (optional explanation for why human review is needed).
